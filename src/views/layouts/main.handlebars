<!DOCTYPE html>
<html lang="fr">
<head>
    <!-- META -->
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />

    <link rel="stylesheet" href="/custome.css">
    <!-- Favicon-->
    <link rel="icon" type="image/x-icon" href="assets/img/favicon.ico" />
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.17.0/font/bootstrap-icons.css" rel="stylesheet">
    <!-- Google fonts-->
    <link href="https://fonts.googleapis.com/css?family=Lato:300,400,700,300italic,400italic,700italic" rel="stylesheet" type="text/css" />
    <!-- TITLE -->
    <title>AideEco - {{title}}</title>
    <style>
        .goog-te-gadget-icon{
            visibility: hidden;
        }
    </style>
</head>
<body>
<div class="container-fluid" style="padding: 0; overflow: hidden">
    <!-- Bootstrap Navbar -->
    <nav class="navbar navbar-expand-lg themeable navbar-dark bg-success" style="height: 7vh">
        <a href="/"> <img src="/assets/img/favicon.ico" alt="Logo" style="width: 5wh; height: 5vh; margin-left: 10px;"> </a>
        <i class="bi bi-lightbulb-fill"></i>
        <div class="container">
            <a class="navbar-brand" href="/">
                <i class="bi bi-house-door-fill"></i> AideEco
            </a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item {{#if home}} active {{/if}}">
                        <a class="nav-link" href="#">Accueil</a>
                    </li>
                    <li class="nav-item {{#if NeRienFaire}} active {{/if}}">
                        <a class="nav-link" href="/NeRienFaire">Version Challenge "Ne Rien Faire"</a>
                    </li>
                     <li class="nav-item dropdown">
                        <button class="btn btn-green dropdown-toggle text-light" data-bs-toggle="dropdown" aria-expanded="false">
                            Thèmes
                        </button>
                        <ul class="dropdown-menu themeable bg-success dropdown-menu-light">
                            <li><a class="dropdown-item text-light" href="#" onclick="changeTheme('default')">Default</a></li>
                            <li><a class="dropdown-item text-light" href="#" onclick="changeTheme('sombre')">Sombre</a></li>
                        </ul>
                    </li>
                </ul>
            </div>
            <div>
                <div class="translate" id="google_translate_element" ></div>
                <script type="text/javascript" src="https://translate.google.com/translate_a/element.js?cb=googleTranslateElementInit"></script>
                <script type="text/javascript">
        function googleTranslateElementInit(a) {
                        let goog = new google.translate.TranslateElement(
                                {pageLanguage:a,
                                includedLanguages: 'fr,en,zh-TW,es,ar,ja,hi,la,pt,ru',
                                layout: google.translate.TranslateElement.InlineLayout.SIMPLE},
                                'google_translate_element')}
                function getUserCountry() {
                    // Obtient la langue du navigateur, qui peut parfois inclure le pays
                    var userLanguage = navigator.language || navigator.userLanguage;

                    // Vous pouvez extraire le pays de la langue
                    var userCountry = userLanguage.split('-')[0] || 'Unknown';

                    // Affiche le pays dans la console (remplacez ceci par une utilisation réelle)
                    console.log('User Country:', userCountry);
                    console.log(document.querySelector("#google_translate_element"));
                 }

                // Appelle la fonction au chargement de la page
                document.addEventListener('DOMContentLoaded', function () {
                     getUserCountry();
                });
                    function changeTheme(theme) {
                        // Sélectionnez tous les éléments ayant la classe 'themeable'
                        var themeableElements = document.getElementsByClassName('themeable');

                        // Parcourez les éléments et changez la classe en conséquence
                        for (var i = 0; i < themeableElements.length; i++) {
                            if (theme === 'default') {
                                // Changez la classe en 'text-dark' pour le thème par défaut
                                themeableElements[i].classList.remove('bg-dark');
                                themeableElements[i].classList.add('bg-success');
                                themeableElements[i].classList.remove('dropdown-menu-dark');
                                themeableElements[i].classList.add('dropdown-menu-light');
                                themeableElements[i].classList.remove('btn-dark');
                                themeableElements[i].classList.add('btn-green');
                            } else if (theme === 'sombre') {
                                // Ajoutez le code nécessaire pour le thème sombre
                                themeableElements[i].classList.remove('bg-success');
                                themeableElements[i].classList.add('bg-dark');
                                themeableElements[i].classList.remove('dropdown-menu-light');
                                themeableElements[i].classList.add('dropdown-menu-dark');
                                themeableElements[i].classList.remove('btn-green');
                                themeableElements[i].classList.add('btn-dark');
                            }
                        }
                    }
                </script>
                
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <main>
        <!-- Content goes here -->
        {{{body}}}
    </main>

    <!-- Bootstrap Footer -->
    <footer class="text-white themeable bg-success text-center py-3" style="height: 7vh; display: flex; justify-content: space-between; align-items: center;">
    <img src="/assets/img/icone_feuille.png" alt="Logo" style="width: 3vw; height: 5vh; margin-right: 10px;">
        <marquee behavior="scroll" direction="left" scrollamount="15" style="font-size: 18px;">Bienvenue sur le site AideEco, le site qui vous permettra de faire le tri entre fausses informations et vraies solutions pour le climat.</marquee>
        <i class="bi bi-lightbulb-fill"></i>
    </footer>

    <!-- Bootstrap JS (optional) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>
</div>
</body>
</html>
